---
const { class: className, classNameToBottom} = Astro.props;
---
<div class={`absolute size-80 rounded-full opacity-20 shadow-2xl shadow-primary z-[-1] blur-[100px] ${className}`}></div>
<div class={`absolute size-80 rounded-full opacity-30 shadow-2xl shadow-primary z-[-1] blur-[100px] ${classNameToBottom}`}></div>