---
import { Form } from "@/features/contact/Form";

const { currentLocale } = Astro;
---

<div class="mx-auto max-w-[1240px] px-6 w-full py-20 md:py-28">
  <Form client:visible currentLocale={currentLocale} />
</div>
